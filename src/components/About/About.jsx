import React from 'react';
import AboutMe from './AboutMe';

const About = () => {
  return (
    <div>
      <AboutMe />
      {/* Footer will automatically be included via the layout */}
    </div>
  );
};

export default About;
